public class Student {
    String name;

    Student(String name) {
        this.name;
    }
    void learn(Teachers b) {
        System.out.println(this.name + "listens to " + b.name);
    }
    void question(Teachers g) {
        System.out.println(this.name + "questions " + g.name);
        k.says(this.)
    }
}
